apiVersion: v1
kind: Service
metadata:
  name: devsecops-mini-service
spec:
  type: NodePort
  selector:
    app: devsecops-mini
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
      nodePort: 30007
